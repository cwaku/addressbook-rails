<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">RokieContact</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Contacts</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Setup
          </a>
          <ul class="dropdown-menu">
            <% if can? :create, Region %>
              <li><%= link_to "Add Region", new_region_path, class: "dropdown-item" %></li>
            <% end %>
            <% if can? :manage, City %>
              <li><%= link_to "Add City", new_city_path, class: "dropdown-item" %></li>
            <% end %>
            <% if can? :create, Suburb %>
              <li><%= link_to "Add Suburb", new_suburb_path, class: "dropdown-item" %></li>
            <% end %>
            <li><hr class="dropdown-divider"></li>
            <% if can? :update, User %>
              <li><%= link_to "Edit User", edit_user_registration_path, class: "dropdown-item" %></li>
            <% end %>
            <% if can? :manage, User %>
              <li><%= link_to "Manage Users", users_path, class: "dropdown-item" %></li>
            <% end %>
            <% if can? :manage, UserRole %>
              <li><%= link_to "Manage User Roles", user_roles_path, class: "dropdown-item" %></li>
            <% end %>
            <% if can? :manage, Contact %>
              <li><%= link_to "Manage Contacts", contacts_path, class: "dropdown-item" %></li>
            <% end %>
            </ul>
          </ul>
        </li>
      </ul>
      <%# Button group for sign in and sign up %>
      <div class="btn-group" role="group" aria-label="Basic example">
        <% if user_signed_in? %>
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary" %>
        <% end %>
      </div>
      <%# <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form> %>
    </div>
  </div>
</nav>