<p style="color: green"><%= notice %></p>

<div id="contacts">
  <% @contacts.each do |contact| %>
    <%= render contact %>
    <p>
      <%= link_to "Show this contact", contact %>
    </p>
  <% end %>
</div>

<%# Create tabble to hold all contacts with pagination %>
<%# Compare this snippet from app/views/contacts/index.html.erb: %>
<div id="contacts">
  <% @contacts.each do |contact| %>
     <%= render contact %>
     <p>
       <%= link_to "Show this contact", contact %>
     </p>
   <% end %>
 </div>

 <div class="table-responsive">
   <table class="table table-striped table-sm">
     <thead>
       <tr>
         <th scope="col">First Name</th>
         <th scope="col">Last Name</th>
         <th scope="col">Phone</th>
         <th scope="col">City</th>
         <th scope="col">Region</th>
         <th scope="col">Remarks</th>
        <th scope="col">Actions</th>
       </tr>
     </thead>
     <tbody>
       <% @contacts.each do |contact| %>
        <tr> %>
          <td><%= contact.firstname %></td>
          <td><%= contact.lastname %></td>
          <td><%= contact.phone %></td>
          <td><%= contact.suburb.city.name %></td>
          <td><%= contact.suburb.city.region.name %></td>
          <td><%= contact.remarks %></td>
          <td>
            <%= link_to "Show", contact %>
            <%= link_to "Edit", edit_contact_path(contact) %>
            <%= link_to "Destroy", contact, method: :delete, data: { confirm: "Are you sure?" } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<%# will_paginate %>


